using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponShoot : MonoBehaviour
{
    // public WeaponBase[] _weapons; //pistol, shotgun, rifle, null;
    // [SerializeField] protected GameObject[] _weaponModels;
    // public WeaponBase _primaryWeapon, _secondaryWeapon, _equippedWeapon;
    // [SerializeField] protected Transform _nozzle, _weaponSlot;
    // [SerializeField] protected GameObject[] _bulletPrefab;
    // [SerializeField] protected AmmoBase[] _ammo;
    // [SerializeField] protected GameObject _activeWeapon, _muzzleFlash;
    // public bool _canFire = true, _isHeld = false, _isReloading;

    // protected virtual void Start()
    // {

    // }

    // protected virtual IEnumerator Fire(int ammoCount)
    // {
        

    //     _currentAmmo--;
    //     if(_currentAmmo <= 0)
    //         StartCoroutine(Reload());

    //     _canFire = false;
    //     yield return new WaitForSeconds(_equippedWeapon._fireRate);
    //     _canFire = true;
    // }

    // private void SpawnBullet(float spread)
    // {

    // }

    // protected virtual IEnumerator Reload()
    // {
    //     _isReloading = true;
    //     yield return new WaitForSeconds(_equippedWeapon._reloadTime * 2);
    //     _isReloading = false;
    // }
}
